# تقرير Final Production Hardening — ⚖️ مكتب الأستاذ / أحمد محمد خضير المحامى

**تاريخ الفحص:** 2026-09-25 (Africa/Cairo) · **إصدار التطبيق:** `3.0.2` · **قاعدة المكتب:** `LawOfficeDB` / v15 · **المصدر:** المشروع العام `law-office-app-stage32-chat-GPT`، فُحصت الشيفرة الفعلية لا تقارير المراحل السابقة.

> **قرار القبول: تسليم مؤهَّل، وليس تصريحًا باجتياز جميع البوابات.** نجحت الاختبارات المبينة أدناه داخل Chromium وملفات تعريف تجريبية. البحث المفهرس الكامل على مجموعة Large، اختبارات الأجهزة المادية، والتحقق التشريعي المستقل لم تجتز بوابة «PASS»؛ لا يجوز وصفها بأنها ناجحة. اسم الملف المطلوب `law-office-app-FINAL.zip` اسم تسليم فقط، لا يغيّر هذا القرار.

## جدول النتائج التنفيذي

| البوابة | النتيجة | الدليل/القيد |
|---|---|---|
| الجرد، البنية، فحص المسارات والاستيرادات، صياغة JavaScript وESLint | **PASS** | `qa/results/static-audit.log`؛ 95 ملف `.js/.mjs` شملت المصدر وأدوات QA، 310 عبارة import إنتاجية، 503 استيرادات مسماة |
| قاعدة جديدة وترقية v1/v5/v10/v13/v14 → v15 وحفظ عينات البيانات | **PASS** | `qa/results/e2e.log`؛ 54 مخزنًا وفهارسها |
| جولات Chromium للوظائف التشغيلية والقفل والنسخ والطباعة وCSV وRTL | **PASS** | E2E 48/48؛ مصفوفة 39/39؛ UI 31/31؛ Extras 27/27؛ المسار الفرعي 7/7؛ ملفات `qa/results/` |
| البحث المتدرج، ثبات الفهرس بعد إعادة الصفحة، المزامنة المشفرة والحذف | **PASS** | Search 16/16؛ Sync-security 15/15 |
| أداة Benchmark مستقلة عن قاعدة المكتب وإيقاف ومسح البيانات | **PASS لـSmall/Medium/Large** | `benchmark-page.log` 6/6؛ `benchmark-scale.log` 6/6؛ Medium 110k ثم Large 1.1M، مسح كل المخازن الاصطناعية وحفظ عميل المكتب |
| حمل Small: 11,000 سجل مع فهرس فعلي | **PASS** | `load-small.log` على الإصدار 3.0.2؛ 3.6 ثوانٍ إدخال، 38.3 ثانية فهرسة |
| حمل Medium: 110,000 سجل مع فهرس فعلي | **PASS** | `load-medium.log`؛ 108.6 ثانية إدخال، 560.5 ثانية فهرسة، 745,979 posting، بحث مفهرس 1417ms |
| حمل Large: 1,100,000 سجل وقراءات وواجهات وتقارير | **PASS للنطاق المقاس** | `load-large-persistent.log`؛ الفهرس النصي لم يُبنَ عمدًا |
| بحث مفهرس كامل على Large | **BLOCKED** | حد تخزين المتصفح: السياق المعزول فشل بـ `QuotaExceededError`؛ الاختبار الدائم قيس دون الفهرس؛ يلزم حصة أكبر وتشغيل الفهرس والتحقق من نتائجه |
| تثبيت Android حقيقي، حوار الطباعة في نظام تشغيل، جهازان ماديان | **NOT TESTED** | استُخدم بديل Chromium/ملفات متصفح منفصلة/ملف PDF؛ يلزم الأجهزة والبيئات الفعلية |
| صحة القواعد القانونية الموضوعية يوم التسليم | **NOT TESTED** | اختُبرت الحقول وإصدار القاعدة وحسابات الأمثلة، لا مراجعة مصدر تشريعي مستقل بواسطة مختص |
| مزامنة فورية بلا نقل ملف بين جهازين | **BLOCKED بالمتطلبات** | لا يوجد Backend بتوجيه المستخدم؛ المنفذ دمج ثنائي **يدوي** لحزم مشفرة، وليس مزامنة لحظية |

## A — نطاق القرار وأساس الثقة

- التزم الفحص بالتسلسل: **SCAN → ARCHITECTURE REVIEW → BUG DISCOVERY → DEPENDENCY CHECK → DATABASE/MIGRATION/PERFORMANCE/SECURITY REVIEW → IMPLEMENT FIXES → AUTOMATED TESTS → BROWSER TESTS → LOAD TESTS → REGRESSION TESTS → FINAL AUDIT → ZIP → REPORT**. جرى الجرد ومراجعة الاعتماديات والطبقات قبل تعديل المصدر، ثم تكررت الاختبارات بعد مجموعات الإصلاح.
- لا تعتمد النتيجة على `README.md` القديم، أسماء ملفات Stage، أو ادعاءات فحوص سابقة؛ توثق أدلة التنفيذ الحالية داخل `qa/results/`. السجلات الموسومة `*-initial.log` أدلة تاريخية لفشل/قيود سابقة، وليست أساس نتيجة الإصدار الحالي.
- لا يوجد تصريح **PASS شامل** لأن عناصر `BLOCKED` و`NOT TESTED` قائمة. لا تُنشر بيانات عملاء حقيقية في ملفات QA أو في الحزمة.

## B — الجرد الفعلي

- واجهة ثابتة `index.html` و`performance-load-test.html`، و`manifest.json` و`service-worker.js`، أربع أوراق CSS، خمس صور PNG (أربع منها في manifest)، 78 وحدة JavaScript تحت `js/`، واختبارات Playwright/Node تحت `qa/`.
- 37 مسارًا مسجلاً في `js/app.js`، و54 مخزن IndexedDB رئيسيًا في `js/core/constants.js` مع تعريفات وفهارس `js/db/schema.js`؛ مسارات الأشخاص والقضايا والربط والعمل القضائي والمالية والحاسبات والمزامنة والنسخ والأرشيف والتقارير ممثلة.
- حزمة Benchmark الاختيارية تفتح `LawOfficeBenchmarkDB` بخمسة مخازن **منفصلة منطقيًا**؛ قاعدة الاختبار والمكتب تشتركان في **حصة تخزين الموقع** ولا تستخدم أي منهما `deleteDatabase()` للترقية أو التنظيف. اجتازت الأداة Small وMedium وLarge، مع فحص العلاقات بعد إعادة التشغيل دون افتراض عودة معرّفات autoIncrement إلى 1.
- تطبيق التشغيل لا يحتاج npm ولا اتصالًا سحابيًا. `qa/server.mjs` خادم ملفات محلي **للاختبار فقط**، وليس Backend أو خدمة مزامنة.

## C — خريطة البنية ومسار البيانات

```text
index.html → js/app.js / router → وحدات الواجهة والحالات والعمليات
                                     ↓
          البحث / التقارير / الحاسبات / backup / sync
                                     ↓
                   js/db/repositories.js (مسار البيانات الوحيد)
                                     ↓
          js/db/db.js → js/db/migrations.js + schema.js → LawOfficeDB v15

service-worker.js → HTML network-first، JS/CSS revalidation، تثبيت موارد offline
performance-load-test.html → repositories.js → db.js → LawOfficeBenchmarkDB
```

- فهرس البحث مشتق من السجلات في `searchIndex` و`searchDoc`؛ النسخة الاحتياطية لا تعتبرهما مصادر أصلية وتعيد بناءهما بعد الاستعادة.
- التحققات المرجعية في التطبيق (`integrity.js` وعمليات الربط): IndexedDB **لا يوفر مفاتيح خارجية حقيقية**. فحص العلاقات والعينات/الفحص الكامل لا يعني أن المتصفح يفرض FK تلقائيًا.
- رسم المسارات متسلسل ويُلغي النتائج المتقادمة؛ تقرير الصفوف الكبيرة يُحسب دون حبس طابور التنقل ويُلغي القراءة عند مغادرة الشاشة.

## D — فحص الشيفرة والاعتماديات

- `node --check` لملفات JavaScript الإنتاجية الـ79 **وملفات QA/config ذات الامتداد `.mjs`** (المجموع 95)، تحقق 310 import إنتاجي و503 اسمًا مستوردًا، وESLint: **PASS**.
- بحث الأنماط `indexedDB.open(` و`getAll(` و`eval(` و`new Function(` و`TODO` و`FIXME`: فتح القاعدتين **فقط** في `js/db/db.js`؛ `getAll()` فقط داخل Repository وبخيار غير محدود صريح، ولا في المسارات الساخنة؛ لا `eval`/`new Function`/TODO/FIXME في مصدر التشغيل؛ لا `indexedDB.deleteDatabase()`.
- تحققت المطابقة: DB_VERSION 15 مع آخر migration؛ التطبيق وService Worker `3.0.2`؛ كل المسارات المسجلة الـ37 لها renderer، وروابط القائمة الـ36 تصل لمسارات مسجلة؛ كل ملفات precache المحلية موجودة **91 أصلًا** وتشمل وحدات JS؛ الأيقونات والمسارات الفعلية، لا الاكتفاء بوجود رابط في الواجهة.
- يعتمد QA على Playwright وChromium وESLint محليًا فقط. لم يجر تدقيق اختراق خارجي أو تدقيق كافة تبعيات npm العابرة؛ **NOT TESTED** كفحص أمني مستقل.

## E — العيوب المؤثرة التي أُصلحت

| المجال | عطل حقيقي قبل الإصلاح | التصحيح المثبت |
|---|---|---|
| التشغيل/الترقيات | بدء القفل قبل فتح DB؛ تمرير معاملات migration بترتيب خاطئ | فتح DB أولًا، وترقيات متسلسلة حتى v15 مع اختبار حفظ البيانات |
| القوائم والعلاقات | دوال صفوف مفقودة، متغير Client 360 غير معرّف، تفكيك Page خاطئ، `poas` غير معرّف | إصلاح العرض وروابط القضية/التوكيل واختبارات النقر والحفظ |
| الأرشفة والإجراءات | استدعاء دالة أرشفة غير موجودة، مسار إجراءات غير مسجل، صفحات لا تتحدث بعد الحفظ | أرشفة فعلية عبر المستودع، تسجيل المسار وتحديث الشاشة |
| البحث | محو postings دون قصد، الاكتفاء بآخر 1000 سجل، اعتبار فهرس جزئي مكتملًا، تقادم كلمات بعد تحديث bulk بنفس العدد | معاملات ذرية للفهارس، تغطية لكل مخزن وعلامة اتساخ دائمة، إعادة بناء آمنة وصفحات نتائج بلا قطع صامت |
| النسخ والمزامنة | قطع scan عند 1000، حزمة مشفرة سربت نسخة plaintext، snapshot لم تُفسَّر صحيحًا، معاينة قديمة | تصدير كامل، تشفير السجلات، تمييز وضعَي الدمج/الاستبدال، إبطال المعاينة عند تغيير الملف أو كلمة المرور |
| الاستعادة | إسكات فشل إعادة فهرسة البحث وإرجاع نتيجة نجاح | التحذير عن فشل الفهرس، الإبقاء على المصدر وإتاحة إعادة البناء |
| المالية/الوقت/القانون | مبلغ عائلي يُقرأ من `amount` بدل `amountMinor`؛ اليوم يتحول عبر UTC؛ بيانات مرجع قاعدة ناقصة | وحدات صغرى صحيحة، تواريخ تقويم ISO محلية، توثيق القواعد والتحقق من الحقول |
| التقارير/التنقل | مسح تجميعي ضخم يحجز التنقل؛ حذف أول preset بالخطأ عند اختيار placeholder؛ قائمة لا تتحدث بعد الحفظ | حساب خلفي قابل للإلغاء، تعطيل الطباعة لحين الاكتمال، ضبط اختيار وحذف الإعدادات |
| PWA/الأداء | أصول JS غير محفوظة لأول تشغيل offline؛ اسم كاش عام قد يحذف مشروعًا آخر؛ رابط Benchmark مفقود | تثبيت كل الموارد، عزل الكاش حسب نطاق التثبيت، إشعار تحديث وحيد، أداة قياس حقيقية منفصلة ومعرّف manifest نسبي داخل نطاق المشروع |
| تكرار اختبار الأداء | `clear()` لا يعيد عداد autoIncrement؛ ربط اصطناعي يفترض المعرف 1 في الجولة التالية | قياس أول معرّف حقيقي لكل تشغيل وربط القضايا والعملاء والجلسات به، مع اختبار Medium ثم Large متتاليين وفحص عدم المساس ببيانات المكتب |

يسرد `README.md` تفاصيل الإصلاحات الأخرى وأماكنها. لا حُذف مسار أو مخزن أو وحدة لمجرد تسهيل نجاح الاختبار.

## F — قاعدة البيانات وRepository والعلاقات

- `indexedDB.open()` حصري في `db.js` للمكتب والاختبار الاصطناعي؛ الوصول التشغيلي إلى المعاملات عبر `repositories.js` فقط. الحذف/الأرشفة والتغييرات القابلة للبحث تحدّث سجل التغيير والفهرس عند اللزوم.
- صفحات Cursor/Index ذات حد، وقراءة `all({limit:null})` أصبحت صريحة وغير محدودة **فقط** عند أعمال مثل backup/fحص شامل؛ لا يجري تحميل 1.1 مليون سجل بواسطة `getAll()` في شاشة قائمة.
- اختبرت QA إنشاء 54 مخزنًا وفهارسها، ربط العميل/القضية/الخصم/التوكيل/الجلسة/الحكم/الطعن، وكشف FK معلق ونقود سالبة وجهة أب مفقودة. لا تُصلح أداة الفحص السجلات بصمت.

## G — الترقيات وعدم فقدان البيانات

- `onupgradeneeded` يستخدم معاملة الترقية الموجودة، ولا يوجد حذف للقاعدة. نجحت ترقية قاعدة **جديدة** إلى v15 وعينات من v1 وv5 وv10 وv13 وv14 إلى v15 مع فحص المخازن والفهارس وبقاء عميل وطعن وتحويل أعلام `archived` إلى `0/1`.
- فشل فتح DB لا يحفظ اتصالًا معطوبًا إلى الأبد؛ التعارض بين تبويبين يعطي رسالة واضحة. النسخة الاحتياطية من إصدار قاعدة أحدث تُرفض لمنع تجاهل مخازنها.
- لم يُختبر كل إصدار وسيط منفردًا، ولا استعادة قاعدة مستخدم مجهولة تحتوي مخططات محرفة؛ **NOT TESTED** خارج الحالات الفعلية أعلاه.

## H — البحث والصفحات والاتساق

- إضافة/تحرير/حذف/استيراد ندّ يحدّث المستندات وpostings؛ تغييرات bulk تكتب علامة `searchDoc` متسخة في نفس معاملة المصدر. التغطية تقارن أعداد الوثائق بالمصادر، ويُحذر المستخدم إذا كان الفهرس ناقصًا.
- اختبار فعلي: 276 نتيجة عربية عبر 10 صفحات بلا تكرار/سقوط؛ بحث AND انتقائي؛ إيقاف بناء الفهرس ثم استئنافه؛ تحديث bulk بنفس عدد الصفوف مع **إغلاق الصفحة وفتحها** لم يُرجع كلمات قديمة على أنها فهرس سليم.
- Medium النهائي أعاد نتائج للعلامة القديمة والحديثة والعربية من الفهرس خلال 1417/17/20ms دون degraded؛ السجل النهائي مثبت في قسم O. Large قيس فيه fallback **محدود وصريح**، ولا يجوز تسميته بحثًا كاملًا.

## I — النسخ الاحتياطي والاستعادة

- نُسخت واستُعيدت **2500/2500** سجلات عميل في اختبار يتجاوز حد الألف؛ تصدير UI لملف JSON وملف مشفر، معاينة ثم تطبيق واسترجاع العميل المؤرشف، وفحص SHA-256/صيغة النسخة. اختُبر العبث بحقل عميل داخل النسخة ورُفض قبل تعديل قاعدة البيانات، وكذلك رُفضت نسخة تعلن DB_VERSION أحدث.
- حزمة الملف المشفر PBKDF2-SHA-256 (200,000 تكرار) + AES-256-GCM مع salt/IV عشوائيين؛ كلمة مرور خاطئة رفضت بلا تعديل بيانات؛ العنوان/بعض metadata العامة لا تدخل في تشفير الحزمة.
- الاستعادة الكاملة تستبدل البيانات **بعد التأكيد ونسخة طوارئ من الواجهة**؛ فشل إعادة بناء فهرس مشتق يُبلَّغ بتحذير لا بنجاح وهمي. **NOT TESTED:** فشل الحصة في منتصف استعادة ضخمة، أو نسخة Large كاملة/التحقق منها؛ يلزم ملف تعريف تجريبي بسعة وذاكرة كافيتين وخطة استرجاع من نسخة الطوارئ. توليد JSON/هاش النسخة الحالية قد يجمع نصًا كبيرًا في ذاكرة المتصفح؛ لا نَدّعي استخدام ذاكرة ثابتة لنسخ 1.1 مليون صف.

## J — المزامنة بدون خادم

- ليس هناك Firebase/Supabase/Backend ولا دفع اشتراك. وضع `merge` يعتمد uid/rev/deviceId وchangeLog وtombstones وحسمًا حتميًا، ويحتفظ بالتعارضات للمراجعة. `snapshot` استبدال صريح وليس دمجًا.
- اختبار Chromium في سياقين معزولين: إرسال A→B، تعديل B→A، منع تطبيق الحزمة مرتين، تعارض، انتشار حذف مشفر B→A مع زوال نتيجة البحث، تطبيق snapshot مشفر وعدّ صحيح، رفض كلمة المرور وتغيير المعاينة.
- **NOT TESTED:** هاتف وحاسوب ماديان بملفات حقيقية ونقل بينهما. **BLOCKED:** مزامنة فورية تلقائية بلا وسيط شبكة أو نقل يدوي؛ تحقيقها يحتاج خادمًا أو وسيلة اتصال صرّح المستخدم بعدم إضافتها. هذه النسخة توفر **مزامنة ملفات ثنائية الاتجاه يدويًا** فقط.

## K — الأمان وحدود الادعاء

- فحص المنابع الثابتة يرفض `eval` و`new Function` وفتح DB خارج طبقتها؛ حقول HTML النصية تُهَرَّب في المسارات المختبرة. هذه فحوص هندسية، **وليست** مراجعة أمنية شاملة أو اختبار اختراق مستقل.
- **PIN قفل للواجهة فقط. IndexedDB على الجهاز ليست مشفرة بهذا الرقم ولا بكلمة مرور النسخة الاحتياطية.** حماية بيانات الجهاز تعتمد على أمان المتصفح/نظام التشغيل وتشفير القرص؛ لا يجوز تسويق الواجهة كحماية من شخص يملك ملف المتصفح.
- لا تسجّل أداة Benchmark سجلات المكتب، لكن استخدام حجم Large على نفس الأصل قد يستنفد حصته؛ شُرط استخدامها على ملف متصفح اختباري خالٍ من بيانات المكتب.

## L — المال والتواريخ والقواعد القانونية

- تخزين المبالغ في وحدات صغرى صحيحة (`amountMinor`، أو حقل دلالي مثل `wageMinor`) مع فحص دقة الإدخال، دعم الأرقام العربية، واختبار نفقة 200.00 − سداد 50.00 = 150.00. لا تخزن كسور عائمة كمبلغ رئيسي.
- التواريخ التقويمية نصوص `YYYY-MM-DD` محلية؛ أزمنة الإنشاء/التعديل ISO UTC. اجتازت أمثلة Africa/Cairo وAmerica/New_York دون انتقال اليوم بسبب UTC.
- مرجع القاعدة يشترط `ruleId`, `source`, `sourceReference`, `sourceUrl`, `effectiveFrom`, `version`, `verificationDate`؛ اختبر 16 قاعدة مدمجة/فهرس، والقاعدة غير الموثقة لا تُحسب، ونسخة قانون الإجراءات الجنائية تتبدل وفق تاريخ `2026-10-01` في المحرك. **NOT TESTED:** صحة النفاذ والتعديلات التشريعية الموضوعية عبر مصدر رسمي مستقل أو مراجعة محامٍ.

## M — واجهة RTL، الاسم وPWA

- الاسم النصي الثابت المعتمد في index/manifest/config/واجهة القفل/PDF/صفحة الأداء/README هو **⚖️ مكتب الأستاذ / أحمد محمد خضير المحامى**؛ الصفحات عربية RTL. شاشة بعرض 390px اجتازت عدم التمرير الأفقي في Chromium؛ جهاز Android حقيقي **NOT TESTED**.
- نجح تسجيل Service Worker داخل Chromium، ومعرّف manifest نسبي يتبع مسار المشروع بدل هوية جذرية مشتركة، وحفظ 91 أصلًا يضم جميع الوحدات، وتشغيل المكتب فعليًا وفتح القضايا وفتح Benchmark **دون شبكة** بعد التثبيت الأول. واختُبر أيضًا مسار محلي فرعي يحاكي `/law-office-app-stage32-chat-GPT/` بنجاح **7/7**، شمل Scope وهوية التثبيت والتشغيل offline، دون الادعاء أن النشر الحقيقي على GitHub Pages اختُبر. اختبار كاش قديم من نطاق التطبيق أزال القديم وأبقى كاش مشروع آخر وأظهر إشعار إعادة التحميل مرة واحدة.
- طباعة A4 إلى PDF في Chromium وتطبيق CSS إخفاء أدوات التحكم: **PASS**؛ حوار الطابعة ونظام Windows/هاتف **NOT TESTED**. لا يُدعى اختبار تثبيت PWA من شاشة Android الفعلية.

## N — مصفوفة الاستخدام الإلزامية في Chromium

جميع الصفوف الآتية **PASS في متصفح Chromium الاختباري فقط**؛ ملفات الإثبات `e2e.log`, `browser-matrix.log`, `ui-flow.log`, `extras.log`. لا يُستنتج من ذلك سلوك كل الأجهزة المادية.

| # | تدفق حقيقي | # | تدفق حقيقي | # | تدفق حقيقي |
|---:|---|---:|---|---:|---|
| 1 | فتح التطبيق | 11 | الإجراءات وربط جلسة | 21 | المركز المالي/`amountMinor` |
| 2 | فتح IndexedDB | 12 | المهمة والإكمال | 22 | التقارير وإعداداتها |
| 3 | إنشاء عميل | 13 | الحكم | 23 | تنزيل CSV لأربعة أنواع |
| 4 | قراءة/تعديل عميل | 14 | الطعن وربطه | 24 | طباعة A4/PDF بالمتصفح |
| 5 | الأرشفة والاسترجاع | 15 | Case 360 | 25 | إنشاء Backup |
| 6 | إنشاء قضية | 16 | Timeline | 26 | معاينة/تطبيق Restore |
| 7 | ربط العميل بالقضية | 17 | Graph وعلاقة قضيتين | 27 | تشفير الملف ورفض كلمة خاطئة |
| 8 | إنشاء خصم وربطه | 18 | بحث عربي وصفحات | 28 | حفظ/تغيير/إلغاء PIN |
| 9 | التوكيل وربطه | 19 | المركز اليومي | 29 | القفل وفكّه |
| 10 | جلسة وحفظها | 20 | التنبيهات | 30 | PWA: manifest/SW/offline/upgrade |

فحص إضافي: حاسبات قانونية ونفقة وعمال، جودة البيانات وتصدير JSON، الأرشيف، التنقل السريع، الاستعادة المشفرة، واختبار صفحة Benchmark للحجم الصغير/الإيقاف والتنظيف وMedium/Large الكاملين في قاعدة اصطناعية منفصلة.

## O — القياسات والأدلة والقيود

**بيئة القياس:** Linux sandbox، Node `20.20.2`، Playwright `1.49.1` مع Chromium headless؛ RAM الجهاز نحو 1.9 GiB دون swap؛ حصة IndexedDB تتغير حسب ملف المتصفح. جميع القياسات تقريبية في هذه البيئة وليست ضمانًا لأجهزة العملاء. `heap` هو JS heap **بعد انتهاء الاختبار** من CDP عندما توفر؛ **ليس ذروة RAM/RSS**؛ `storage` تقدير حصة الأصل، وليس حجم ملف DB بالضبط.

| الحجم | العملاء/القضايا/الجلسات/الإجراءات/المهام | إدخال | بناء فهرس نصي | صفحة50؛ بحث مفهرس | تقارير كاملة؛ تنقل عند الإلغاء | التخزين/الحصة؛ heap بعد الاختبار | النتيجة |
|---|---|---:|---:|---|---|---|---|
| Small | 1k/1k/3k/5k/1k = 11,000 | 3.6s | 38.3s، 74,543 postings | 6ms؛ 177ms دون degraded | 296ms؛ غير مطلوب | 14/107 MB؛ 4 MB | **PASS** |
| Medium | 10k/10k/30k/50k/10k = 110,000 | 108.6s | 560.5s، 745,979 postings | 3ms؛ 1417ms دون degraded | 1.543s؛ غير مطلوب | 83/132 MB؛ 4 MB | **PASS** |
| Large | 100k/100k/300k/500k/100k = 1,100,000 | 173.5s | **NOT TESTED** | 4ms؛ البحث المتدهور ليس مفهرسًا | 39.291s؛ 38ms للتنقل والإلغاء | 195/595 MB؛ 3 MB | **PASS للنطاق، INDEX BLOCKED** |

**اختبار منفصل لزرّي Medium وLarge في صفحة Benchmark نفسها** (`qa/results/benchmark-scale.log`): استخدم قاعدة اصطناعية *غير* `LawOfficeDB` داخل ملف تعريف دائم تجريبي، ثم مسح المخازن الخمسة بعد كل تشغيل. هذه القراءة تختبر فهارس الربط والصفحات فقط؛ **لا تبني فهرس البحث النصي** ولا تعوّض حد Large أعلاه.

| زر الأداة | الصفوف المكتوبة | الكتابة | صفحة 50 / 20 صفحة عميقة | تخزين الأصل أثناء القياس | النتيجة |
|---|---:|---:|---:|---:|---|
| Medium | 110,000 | 12.534s | 3ms / 35ms | 21/595 MB | **PASS؛ تنظيف 5/5** |
| Large | 1,100,000 | 136.977s | 3ms / 34ms | 107/595 MB | **PASS؛ تنظيف 5/5** |

تحققت QA من معرّفات العملاء والقضايا **الفعلية** بعد الجولة الأولى (`clear()` لا يصفّر autoIncrement)، وعدد الجلسات المرتبطة بالقضية، وبقاء العميل التجريبي في قاعدة المكتب دون تعديل. قيمة `heapMB` التي تُظهرها صفحة الأداة ناتجة عن `performance.memory` وقد تكون مقربة؛ ليست قياس ذروة RAM.

- Medium النهائي في سياق منعزل: أكمل 110,000 صف ثم بناء 745,979 posting، وبحث السجلات القديمة/الحديثة/العربية دون fallback، وتقاريرها الكاملة خلال 1.543 ثانية؛ `qa/results/load-medium.log` يثبت `errors: (none)`. سجل `load-medium-interrupted.log` يخص تشغيلًا توقّف عند استئناف مساحة العمل قبل انتهاء الفهرسة، وليس نتيجة تشغيل نهائي.
- Large في سياق منعزل بحصة صغيرة تعذر إدخاله (`QuotaExceededError` في `qa/results/load-large-isolated.log`). في ملف دائم بحصة 595MB نجح إدخال 1.1M صف، كل الشاشات السبع والقياسات أعلاه، لكن بُني **0** posting عمدًا بخيار `SKIP_INDEX=1`. في Medium ارتفع تخزين الأصل تقديريًا من 21MB بعد الإدخال إلى 83MB بعد الفهرسة (+62MB لنحو 746 ألف posting)؛ استقراء خطي **غير مضمون** إلى نحو 7.46 مليون posting يتجاوز ما تبقى من حصة Large بعد إدخال 195MB. لم نُجرِ بناء الفهرس الكامل على ملف Large الدائم أو ندّعِ أنه فشل فيه فعليًا؛ يلزم ملف تعريف بحصة أكبر ووقت بناء واختبار والتحقق من السجلات القديمة ثم قياس الذاكرة/القرص؛ البوابة **BLOCKED / NOT TESTED** في هذه البيئة.
- مسح البحث المتدهور على Large أعاد نتيجة حديثة واحدة لكنه لم يجد العلامة القديمة؛ `searchHits=0`, `searchArHits=0` في السجل. هذا **ليس PASS للبحث الشامل**؛ الواجهة تُظهر بوضوح أن fallback محدود مؤقتًا.

## P — إعادة التشغيل، المخاطر المتبقية والتسليم

**الاختبارات التي نجحت فعلًا:** `qa/results/static-audit.log`؛ `e2e.log` (48/48)؛ `browser-matrix.log` (39/39)؛ `ui-flow.log` (31/31)؛ `extras.log` (27/27 بعد فحص معرّف manifest والكاش المحدد بالنطاق)؛ `legal.log` (10/10)؛ `search-consistency.log` (16/16)؛ `sync-security.log` (15/15؛ يتضمن رفض نسخة أحدث وSHA-256 غير مطابق قبل تغيير البيانات)؛ `navigation-race.log` (3/3)؛ `benchmark-page.log` (6/6)؛ `benchmark-scale.log` (6/6، Medium/Large والتأكد من العلاقات والتنظيف)؛ `nested-scope.log` (7/7)؛ `load-small.log`؛ `load-large-persistent.log` للنطاق المحدد. `load-medium.log` (PASS، 110,000 صف وفهرس كامل).

**NOT TESTED/BLOCKED بلا استثناء مخفي:** فهرسة/بحث Large الكامل (BLOCKED بالحصة؛ اختُبر بدلًا منه الإدخال/صفحات/تقارير/fallback المتدهور)، نسخ/استعادة Large (NOT TESTED؛ يلزم مساحة/ذاكرة ونسخة بيانات اصطناعية آمنة)، Android وتثبيت PWA الفعلي وحوار الطابعة النظامي (NOT TESTED؛ بديل Chromium/offline/PDF)، جهازان ماديان (NOT TESTED؛ بديل سياقين)، صحة القانون المستقلة (NOT TESTED)، فحص اختراق/تبعيات خارجي (NOT TESTED)، مزامنة لحظية بلا خادم (BLOCKED بالمتطلب المعماري). لا تحمي PIN قاعدة IndexedDB؛ ولا توجد دعوى بذلك.

**خطوات إعادة التشغيل محليًا:** `cd qa && npm ci && npx playwright install chromium` ثم في طرفية `npm run serve`، وفي أخرى `npm run static && npm run e2e && npm run ui && npm run extras && npm run matrix && npm run legal && npm run search && npm run sync-security && npm run navigation && npm run benchmark-page && npm run benchmark-scale && npm run nested && npm run load:small && npm run load:medium`؛ لاختبار Large في ملف تعريف تجريبي فقط: `PERSISTENT=1 SKIP_INDEX=1 node load.mjs large`. لا تشغّل أحجام الاختبار على ملف المتصفح الذي يحفظ بيانات المكتب الحقيقية.

**تحذير ترقية PWA:** تغيير معرّف manifest من `/law-office-app` إلى هوية نسبية للمشروع يمنع تصادم مشاريع GitHub Pages، لكنه قد يُظهر إدخال تثبيت جديدًا؛ انسخ البيانات مشفرة وتحقق من النسخة قبل إزالة أي تثبيت قديم. **التسليم:** `law-office-app-FINAL.zip` يحتوي المشروع الكامل ومصدر QA وسجلاته ونسخة من هذا التقرير، دون `.git/`, `qa/node_modules/`, أو ملفات تعريف متصفح اختبارية. `FINAL-REPORT.md` نسخة مستقلة للقراءة. قبل نشره على GitHub Pages: انسخ ZIP إلى فرع/مسار التوزيع الثابت، احتفظ بنسخة مشفرة خارج الجهاز، تحقق من HTTPS ومسار Scope الخاص بالمشروع، واطلب تحقق القواعد القانونية والاختبارات المادية قبل اعتماد إنتاج غير مشروط.
