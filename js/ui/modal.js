export function confirmModal({title='تأكيد',message='هل أنت متأكد؟',confirmText='تأكيد',danger=false}={}){return new Promise(resolve=>{const root=document.getElementById('modalRoot');const wrap=document.createElement('div');wrap.className='modal-backdrop';wrap.innerHTML=`<section class="modal" role="dialog" aria-modal="true" aria-labelledby="modalTitle"><div class="modal-header"><h2 id="modalTitle"></h2><button class="icon-button" type="button" data-cancel>×</button></div><p></p><div class="form-actions"><button type="button" class="${danger?'danger-button':'primary-button'}" data-confirm></button><button type="button" class="secondary-button" data-cancel>إلغاء</button></div></section>`;wrap.querySelector('h2').textContent=title;wrap.querySelector('p').textContent=message;wrap.querySelector('[data-confirm]').textContent=confirmText;const finish=v=>{wrap.remove();resolve(v)};wrap.querySelectorAll('[data-cancel]').forEach(b=>b.addEventListener('click',()=>finish(false)));wrap.querySelector('[data-confirm]').addEventListener('click',()=>finish(true));wrap.addEventListener('click',e=>{if(e.target===wrap)finish(false)});root.appendChild(wrap);wrap.querySelector('[data-confirm]').focus()})}
