import {APP_CONFIG} from '../config.js';import {migrate} from './migrations.js';import {AppError} from '../core/errors.js';
let connection=null;export function openDB(){if(connection)return connection;connection=new Promise((resolve,reject)=>{if(!('indexedDB' in globalThis)){reject(new AppError('المتصفح لا يدعم IndexedDB.','NO_INDEXEDDB'));return}const request=indexedDB.open(APP_CONFIG.dbName,APP_CONFIG.dbVersion);request.onupgradeneeded=e=>{try{migrate(request.result,e.oldVersion,request.transaction)}catch(err){request.transaction?.abort();reject(err)}};request.onsuccess=()=>{const db=request.result;db.onversionchange=()=>db.close();resolve(db)};request.onerror=()=>reject(request.error||new Error('تعذر فتح قاعدة البيانات.'));request.onblocked=()=>reject(new AppError('قاعدة البيانات مشغولة في نافذة أخرى. أغلق النسخة الأخرى ثم أعد المحاولة.','DB_BLOCKED'))});return connection}export function getDB(){if(!connection)throw new AppError('قاعدة البيانات لم تُفتح بعد.','DB_NOT_READY');return connection}export async function closeDB(){if(connection){const db=await connection;db.close();connection=null}}
