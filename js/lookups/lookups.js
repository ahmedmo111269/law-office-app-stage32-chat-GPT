import {STORES} from '../core/constants.js';import {repo} from '../db/repositories.js';let cache={};export async function loadLookups(){cache={};for(const row of await repo(STORES.lookups).all({ limit: 500 })){if(!row.active)continue;(cache[row.category]??=[]).push(row)}for(const key of Object.keys(cache))cache[key].sort((a,b)=>(a.sortOrder??0)-(b.sortOrder??0));return cache}export function getLookup(category){return cache[category]||[]}export function clearLookupCache(){cache={}}
